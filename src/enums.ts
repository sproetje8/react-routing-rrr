export enum EPageName {
  REUSE = 'reuse',
  REDUCE = 'reduce',
  RECYCLE = 'recycle'
}

export enum EControlHeader {
  WHAT = 'what',
  HOW = 'how',
  MORE_INFORMATION = 'more-information'
}